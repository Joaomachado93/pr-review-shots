# PR review shots

Fixture-only screenshots used to embed inline in review comments on the private
`gomagaming/sportsbook-frontend-demo` repo (private-repo raw URLs don't render
through GitHub's camo proxy in comments, so a public host is needed).

All content is mock/fixture data — no real user info, no API keys.

- `pr850/` — INC-625 register autofill fix (`Protected` default handling)
- `pr849/` — INC-681 cool-off detection (`selfExclusion`-wrapped DTO)
